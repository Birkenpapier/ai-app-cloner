// AUTO-GENERATED by scripts/gen-backend.mjs from docs/research/app-spec.example.json (dataModel).
// Do not edit by hand. Re-run: node scripts/gen-backend.mjs
//
// drizzle-zod validators drive the tRPC write path. zod is imported from 'zod/v4'
// to match drizzle-zod 0.8 (which builds zod-4 schemas internally).
import { createSelectSchema } from 'drizzle-zod';
import { z } from 'zod/v4';
import { folders, notes } from './schema';

const folderRow = createSelectSchema(folders);
export const folderCreate = folderRow.omit({ id: true });
export const folderUpdate = folderCreate.partial();

const noteRow = createSelectSchema(notes, { color: z.enum(['default', 'yellow', 'green']) });
export const noteCreate = noteRow.omit({ id: true });
export const noteUpdate = noteCreate.partial();
